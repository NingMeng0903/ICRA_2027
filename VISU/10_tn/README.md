# 10_tn — 只观察 PROB (10) 第一项

采集：`20260903_195831` · 对 `DATA/03_chirp/chirp.csv` 观察 · 钉死 03：**T0=28 ms，Tp=14 ms，K=0.96** · 不开 CDYOB

## 结论

- **Vm − Tn Γd Vi 的 RMSE = 1.63 mm/s**（p95 3.2，max 8.2）。空气里小，Tn 能解释内环。
- 同一条 chirp 上，不建模的瞬时 `vel_ff − v_ach` 是 **8.2 mm/s**（05 那档）。差的是 T0 相位；模型吃掉延迟之后残差掉到约 1/5。
- 手扫带：0.3–1 Hz 模型 1.5 / 生误差 2.3；**1–3 Hz 模型 1.4 / 生误差 5.6**。3–8 Hz 仍约 1.9。残差几乎不随频率涨，剩下的是摩擦和一点增益，不是没建到的死区。
- 这就是 \(V/V_r|_{Q=1}=T_n\)。没有辨识 Q / N1 / N2，也没有 apply。不要写 yaml。

数据：`DATA/10_tn/`
