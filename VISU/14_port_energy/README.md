# 14_port_energy — 真实端口与前缀能量

本轮**还没采**。比较命令功、延迟命令功、实际 twist 功和位姿增量功，并检查先输出后回收的前缀债务。

失败则主文取消无源性声明。有限罐会被摩擦耗尽。

```bash
python FORCE_TEST/14_port_energy.py --window-a-csv /path/to/window_a.csv --dry-run
```
