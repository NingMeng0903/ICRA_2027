# 03_chirp — 内环速度 FRF

采集：`20260903_163352` · filter OFF · 力环关 · `15` mm/s chirp · `vel_ff → v_ach`

## 结论

- **T0 = 28.0 ms，Tp = 14.0 ms，K = 0.960**（FRF 拟合，γ²≥0.6，≤8 Hz）。
- 有效带 **0.3–8.0 Hz**（只计扫频以内）。手扫 0.3–3 Hz 足够。
- 时域 xcorr = 40 ms **不是 T0**。
- 15 mm/s 已够：0.3–8 Hz 相干约 0.98。再加大激励会在高频顶加速度饱和，帮不上忙。
- 不要用这组数覆盖 yaml，除非明确要写。8 月 29 日空气辨识仍是 T0=35 ms / Tp=12 ms。本轮相位更贴 28+14。

## 图

`gv_bode.png`：对数频率，刻度 0.2 / 0.5 / 1 / 2 / 5 / 8 / 10 Hz。曲线可画出 8 Hz 一点；拟合和有效带只到 8 Hz。顶上 “8 Hz cut-off” 标截止，图上不画阈值虚线。

- **a** — |Gv|（实线实测，虚线 FOPDT）
- **b** — 相位
- **c** — 相干

数据：`DATA/03_chirp/`
