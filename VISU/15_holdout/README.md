# 15_holdout — 独立覆盖

本轮**还没采**。不辨识新模型。只随机采不同 \(K_e,F_0,v,\omega_\theta,\rho\), direction，问 08/11–14 的管子能不能盖住。分析不会写出新的 \(G,K_e,H,\bar w\)。

先把 07–14 采完再跑。

```bash
python FORCE_TEST/15_holdout.py --window-a-csv /path/to/window_a.csv --dry-run
```
